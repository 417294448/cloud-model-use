# 2026-09-22 模型数据变更

## Qwen / 通义千问（更新）

官方 models 总览 + billing 价格页 + `asr-model` / omni / s2s 分类文档核对，新增 5 个模型（在库模型的上下文与价格 **0 处不一致**）：

- 新增 `qwen-audio-3.1-asr-flash-streaming`（实时识别旗舰）：WebSocket、热词+Prompt 上下文、无时长限制；6元/4.5元 每百万 Token
- 新增 `qwen-audio-3.1-asr-flash-filetrans`（文件转写旗舰）：HTTP、支持说话人分离、12 小时/2GB；0.8元/2.7元
- 新增 `qwen-audio-3.1-asr-flash`：HTTP、短音频、支持说话人分离、5 分钟/2GB；0.8元/2.7元
- 新增 `qwen3.8-omni-flash-realtime`（全模态实时版）：Realtime API（WebSocket/WebRTC/AOQ），文本/图片/视频输入 1.5元、音频输入 6元、文本输出 4.5元、音频输出 12元
- 新增 `qwen-audio-3.1-realtime-plus`（S2S 旗舰）：WebSocket，文本输入 5元 / 音频输入 40元 / 文本输出 40元 / 音频输出 150元
- 官方总览「语音识别」「语音转语音」「全模态」三处推荐位已切至 3.1 系列与 `qwen3.8-omni-flash-realtime`；上代 3.0 三行徽章由 NEW/推荐 降为无标记、说明改注「上代」
- 快速选型「实时语音对话」→ `qwen-audio-3.1-realtime-plus`、「语音听录」→ `qwen-audio-3.1-asr-flash-filetrans`；能力矩阵对应两行同步上移；命名规律示例同步为 3.1
- 收录模型 147 → **152**；同步日期 2026-09-20 → 2026-09-22（zh/en）
- **未收录（待确认）**：billing 页出现的 `qwen-audio-3.1-asr-flash-message`（6元/4.5元）与 `qwen-audio-3.1-tts-next`（6元/12元）在官方分类文档中暂无能力说明，参数无法核实，本次不收录
- 通道说明：`speech-recognition` 页已改为目录页（正文拆至 `asr-model` 等子页，旧抓取只得空壳）；本次改从 `asr-model` 页取 ASR 能力，`sensevoice-v1` / `gummy-*` 仍在该页「即将下线」清单

## Gemini（更新）

`ai.google.dev/gemini-api/docs/models`（Last updated 2026-09-22）与 `pricing`（Last updated 2026-09-16）直连核对：

- 新增 `gemini-3.8-live`（Stable）：默认 Live 模型，异步工具调用（工具/API 后台执行）；文本输入 $0.75 / 输出 $4.5（音频 $3/$12）
- 新增 `gemini-3.8-live-extended-thinking`（Stable）：边说话边后台推理，多步任务不打断；价格同上
- `gemini-3.1-flash-live-preview` 官方已标注 Legacy 并建议迁移 3.8 Live：NEW 徽章移除、说明改注「上代」
- 快速选型 / 能力矩阵「实时语音对话」推荐位 → `gemini-3.8-live`（备选 `gemini-3.8-live-extended-thinking`）；命名规律示例同步
- 收录模型 57 → **59**；同步日期 2026-09-05 → 2026-09-22（zh/en）
- 通道说明：公共代理须对目标 URL 整体 URL-encode（`api.allorigins.win/raw?url=<encoded>`），否则返回 52x；models / pricing 两页均已抓到并含官方 Last updated

## OpenAI（复核）

`developers.openai.com` 列表页（100 slug，0 失败）+ 详情页 + `.md` 全量抓取，`fill_objective_fields.py` 回填：**0 处档位差异**。data 收录 87 个模型 ID；官方列表多出的 18 个（`babbage-002`、`davinci-002`、`text-moderation-*`、`chatgpt-image-latest`、`gpt-4o*-preview`、`gpt-5*-chat-latest`、`omni-moderation-latest` 等）均为本页未收录的历史/工具/别名模型，**无新增/移除**。数据核对一致，无变更。

## Anthropic（复核）

文档站直连抓取 pricing（567KB）/ model-deprecations（426KB）/ models/overview（373KB）：

- 现役 11 个模型与官方 pricing 表逐行一致（Fable 5.1 / Fable 5 / Opus 5 / 4.8 / 4.7 / 4.6 / 4.5 / Sonnet 5 / 4.6 / 4.5 / Haiku 4.5），价格与档位全部一致（Mythos 5.1 / Mythos 5 按既有口径不入主表）
- 退役表 12 行与官方逐条一致（含 `claude-opus-4-1-20250805`、`claude-sonnet-4-20250514`、`claude-2.0` 等）；官方退役页的 `claude-mythos-preview`（Deprecated）与 claude-1.x / instant 历史条目属主表未收录范围，按弃用表收录规则不入表
- 数据核对一致，无变更

## Z.ai / 智谱（复核）

- 模型概览页 9 款核心模型（GLM-5.3 / 5.3-Flash / 5.3-FlashX / 5.2 / GLM-Image / GLM-OCR / GLM-ASR-2512 / GLM-TTS / CogVideoX-3 / Embedding-3）及各分类清单与 data **全部在库，无新增**
- 价格页仍为**通道退化**状态（Playwright 仅渲染出 GLM-5.3 / 5.3-Flash / 5.2 / 5.1 / GLM-OCR / GLM-TTS 6 个模型卡片），`update_data.py` 据此报出的 35 条「可能下架」**全部为误报**（被报模型在模型概览页仍在），**未删除任何模型**；「Rerank 最大输出」变更同为解析串行残留。下轮建议给 pricing 抓取加「渲染模型数下限」校验
- 数据核对一致，无变更

## index.html

- `sync_index_stats.py --write` 汇总：Vendors 5、Models **370+**（各厂商加和 379 → 向下取整到 10）、Max context 10M，与现有 `index.html` 完全一致，**无差异**；厂商清单与 Tab 未变，index.html 无需改动
